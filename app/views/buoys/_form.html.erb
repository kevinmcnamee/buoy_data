<%= form_for(@buoy) do |f| %>
  <% if @buoy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@buoy.errors.count, "error") %> prohibited this buoy from being saved:</h2>

      <ul>
      <% @buoy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :coordinates %><br />
    <%= f.text_field :coordinates %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
